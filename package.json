{
  "name": "linebreakr",
  "version": "1.0.0",
  "scripts": {
    "precommit": "npm test",
    "build": "esbuild src/index.js --bundle --minify --outfile=dist/index.js",
    "test": "jest"
  },
  "type": "module",
  "jest": {
    "transform": {
      "^.+\\.js$": "babel-jest"
    }
  },
  "author": "clhilgert",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@babel/preset-env": "^7.26.0",
    "babel-jest": "^29.7.0",
    "esbuild": "^0.24.0",
    "jest": "^29.7.0",
    "pre-commit": "^1.2.2"
  }
}
