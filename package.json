{
  "name": "linebreakr",
  "version": "1.0.0",
  "scripts": {
    "precommit": "npm test",
    "build": "esbuild src/index.js --bundle --minify --outfile=dist/index.js",
    "test": "jest"
  },
  "author": "clhilgert",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jest": "^29.7.0"
  }
}
